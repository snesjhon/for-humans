// Exercise 3: Optional Fields
// Add an optional lastSeenAt field and handle its absence.

// TODO: Define the Device interface with:
//   readonly id: string
//   name: string
//   isOnline: boolean
//   createdAt: string
//   lastSeenAt?: string   <-- optional

// TODO: Write getLastSeen(device: Device): string
//   Returns device.lastSeenAt when present.
//   Returns 'never' when absent.

export {};
